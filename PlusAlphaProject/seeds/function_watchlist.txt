# Observador multi-PC de alcance em gameplay.
#
# Formato: um endereco formal alinhado por linha. Texto depois de # e ignorado.
# Limite do runtime: 128 enderecos fisicos unicos.
# Esta lista NAO promove seeds e NAO autoriza geracao de fontes.
# Baseline: checkpoint Release S1-255. Campanha dirigida para preparar S1-256.
# As cinco primeiras funcoes formam a closure direta residual de 586 palavras.
# As seis seguintes sao destinos da tabela indireta usada pelo JALR em
# 0x8019209C. Elas permanecem fora da closure direta e serao apenas observadas.

0x8016FC28  # raiz residual; 39 palavras; 9.888 hits interpretados conhecidos
0x801910A4  # folha direta; 141 palavras; 16.068 hits interpretados conhecidos
0x801914C0  # folha direta; 50 palavras; 2.471 hits interpretados conhecidos
0x80191C84  # closure principal; 297 palavras; dois JALR
0x80192D6C  # folha direta; 59 palavras; 1.728 hits interpretados conhecidos

0x80192128  # JALR state[0]; fora dos ranges S1-255
0x80193174  # JALR state[1]; fora dos ranges S1-255
0x801930BC  # JALR state[2]; fora dos ranges S1-255
0x8019319C  # JALR state[3]; fora dos ranges S1-255
0x801931C4  # JALR state[4]; fora dos ranges S1-255
0x80192F60  # JALR state[5]; fora dos ranges S1-255
